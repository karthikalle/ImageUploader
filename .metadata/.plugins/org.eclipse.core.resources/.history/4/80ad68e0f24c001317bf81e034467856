package dataEntryTesting;

import java.awt.Frame;

import javax.swing.JFrame;

import org.fest.swing.edt.FailOnThreadViolationRepaintManager;
import org.fest.swing.edt.GuiActionRunner;
import org.fest.swing.edt.GuiQuery;
import org.fest.swing.fixture.FrameFixture;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;
import org.fest.swing.testng.testcase.FestSwingTestngTestCase;

import dataEntry.GUI_DataEntry;

public class AssemblyTestCase {
	private FrameFixture testFrame; 
    private Frame  frame;
    private GUI_DataEntry gde;
    
    @BeforeClass public static void setUpOnce() {
        FailOnThreadViolationRepaintManager.install();
      }
	
	 @Before 
	    public void setUp()  { 
		 gde = new GUI_DataEntry();
	        gde.frame =  (JFrame) GuiActionRunner.execute(new GuiQuery<Frame>() { 
	            protected Frame executeInEDT() { 
	            	return frame;
	            }
	        }); 
	        testFrame = new FrameFixture(frame); 
	        testFrame.show(); 
	    }

	
		@Test 
		public void shouldCopyTextInLabelWhenClickingButton() {
		    testFrame.textBox("imageName").enterText("Some random text");
		    testFrame.button("copyButton").click();
		    testFrame.label("copiedText").requireText("Some random text");
		  }
	
}
