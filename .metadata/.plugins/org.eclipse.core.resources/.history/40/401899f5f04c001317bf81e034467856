package dataEntryTesting;

import org.fest.swing.edt.FailOnThreadViolationRepaintManager;
import org.fest.swing.edt.GuiActionRunner;
import org.fest.swing.edt.GuiQuery;
import org.fest.swing.fixture.FrameFixture;
import org.junit.Before;
import org.junit.BeforeClass;

import org.fest.swing.testng.*;

import dataEntry.GUI_DataEntry;

public class AssemblyTestCase extends {
	private FrameFixture testFrame; 
    private MyFrame  frame;
    
	 @BeforeClass
	    public static void setUpOnce() {
	        FailOnThreadViolationRepaintManager.install();
	    }
	
	 @Before 
	    public void setUp()  { 
	        frame =  GuiActionRunner.execute(new GuiQuery<AllTypesFrame>() { 
	            protected AllTypesFrame executeInEDT() { 
	                return new GUI_DataEntry(); 
	            }
	        }); 
	        testFrame = new FrameFixture(frame); 
	        testFrame.show(); 
	    }
}
